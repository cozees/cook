include "Cookfile.debug"

var = 123
VAR = 123
VAR = 123 +
      124 -
      383
V = A / B
V = A/B
V = "String1 $V ${V} \v String2 String3"
V = 'String1 ${VAR} String2 String3'
VAR1 = 1.32 + var
V1 = V
FILE = "test/sample/txt"
ARRAY1 = [V1, V2, V3]
MAP = {123: V, V: var}

initialize:

initialize@window:
    FILE = "C:\\test\\sample\\112.txt"

finalize:

all: *

TARGET1: @workin test/sample
    // calling TARGET
    @TARGET
    for index in 1..VAR {}
    for index in 1..40 {}
    for index, val in ARRAY {}
    for key, val in MAP {}
	v = 1
    for {
		if v > 3 {
        	break
		} else if v == 2 {
        	continue @TARGET
		}
		v++
    }
    if VAR == 123 {
        @TARGET -p
    } else if VAR == abc {
        @TARGET1
    } else {

    }

TARGET@window:
TARGET@macos:
TARGET@linux:
TARGET:
    // Single line comment
    /* Multiple line comment */
    @GET http://www.example.com/$VAR > $FILE
    #ls
    if VAR == 234 {
        
    } else {

    }